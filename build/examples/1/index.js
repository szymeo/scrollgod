'use strict';

const scrollGod = new ScrollGod.Square({
    parentSelector: '#scrollgod',
    elemSelector: '.scrollgod-apply',
    speed: '1000', // in miliseconds
    preserveUniqScroll: true,
    cardStyle: {
        'border-radius': '4px'
    }
})
console.log('opt') 
    